import { DataexchangePipe } from './dataexchange.pipe';

describe('DataexchangePipe', () => {
  it('create an instance', () => {
    const pipe = new DataexchangePipe();
    expect(pipe).toBeTruthy();
  });
});
